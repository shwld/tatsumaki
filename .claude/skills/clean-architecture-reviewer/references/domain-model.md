# ドメインモデル

- 永続化エンティティの監査項目として `createdAt` と `updatedAt` を必須とする。
- ドメイン entity は `__typename`（例: `Project`）を必須にし、境界を跨ぐ際の型識別を明示する。
- ドメインの制約条件（必須・範囲・状態遷移・一意性など）のバリデーションはサーバー側（usecase/domain 層）で必ず実施する。フロントエンドのバリデーションは UX 向上のための補助であり、信頼境界にはならない。フロントエンドだけで制約を検証してサーバー側が無検証のまま永続化する実装は禁止する。
